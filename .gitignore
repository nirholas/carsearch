node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
__pycache__/
*.pyc
.venv/
# SQLite writes two sidecar files next to the database in WAL mode, and ignoring
# only the .db leaves those two tracked.
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite
data/probe-*.json
data/run-*.json
data/run-*.log

# Playwright screenshots from UI verification runs.
data/ui-*.png

# Cached EPA lookups: derived data, regenerable, and large.
data/epa-cache.json
data/*.png
data/*-cache.json
data/kbb-index-done.txt
data/kbb-index.log

# Generated index artifacts. The vocabulary is the exception: crawl scripts read
# it to know which makes exist, so a fresh clone needs it committed.
data/*
!data/vehicle-vocab.json
data/full-index-done.txt
data/full-index.log
